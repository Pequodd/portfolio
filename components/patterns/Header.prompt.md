Хедер сборника: единственная навигация в проекте.

```jsx
<Header filters={['Все','Многостраничные','Лендинги','Tilda','С нуля','Редизайн']} active="Все" onFilter={setTag} />
```

На странице кейса фильтры опускаются — остаётся только знак.
