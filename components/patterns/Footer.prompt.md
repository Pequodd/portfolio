Один футер, одна кнопка, один призыв — второго CTA в сборнике не бывает.

```jsx
<Footer line="Расскажите про задачу — предложим, как её решить"
  contacts={[{ value: 'telegram', href: '#' }, { value: 'почта', href: '#' }]} />
```

Ghost-вордмарк внизу — цвет #1F1F1F на #0E0E0E, почти неразличимо.
