/** Волосяная линия 1px #E3E3E1 — единственный разделитель системы. */
export interface RuleProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare function Rule(props: RuleProps): JSX.Element;
