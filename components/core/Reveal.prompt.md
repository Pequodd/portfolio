Вторую анимацию в систему не добавлять: ни параллакса, ни привязки к скроллу, ни наклонов при наведении.

```jsx
<Reveal index={2}><MetaRow ... /></Reveal>
```

`index` задаёт сдвиг 80ms между соседними элементами одного экрана.
